{% extends 'backend.html.twig' %}

{% block contenido %}
    <h1>Usuario creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('usuariocrud_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
